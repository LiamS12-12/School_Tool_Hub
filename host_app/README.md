# Host App

This folder is the start of the shared school platform shell.

Goals of this MVP host:
- provide one consistent app shell
- define a shared navigation model
- define how mini apps register with the host
- create a clean place for shared school data and shared UI

The existing `class_bank` folder is still separate for now and will later be migrated into `host_app/modules/classroom-economy`.
